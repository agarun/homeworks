<h1><%= @cat.name %>'s Page</h1>

  <table>
    
    <tr><th>Atttribute</th><th>Values</th></tr>
    
    <tr>
      <td>Age</td>
      <td><%= @cat.age %></td>
    </tr>
    <tr>
      <td>Birthday</td>
      <td><%= @cat.birth_date %></td>
    </tr>
    <tr>
      <td>Color</td>
      <td><%= @cat.color %></td>
    </tr>
    <tr>
      <td>Sex</td>
      <td><%= @cat.sex %></td>
    </tr>
      <tr>
        <td>Description</td>
        <td><%= @cat.description %></td>
      </tr>
  </table>

  <table>
    
    <tr><th>User</th><th>Start Date</th><th>End Date</th><th>Status</th></tr>
    
    <% @cat.rental_requests.order(:start_date).each do |request| %>
      <tr>
        <td>"no user class"</td>
        <td><%= request.start_date %></td>
        <td><%= request.end_date %></td>
        <td><%= request.status %></td>
      </tr>
    <% end %>

  </table>