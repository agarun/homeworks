// window.setTimeout(() => alert('HAMMERTIME'), 5000);

// const hammerTime = function hammerTime(time) {
//   setTimeout(() => alert(`${time} is hammertime!`), time); // closure over `time`
// };
// hammerTime(2000);
